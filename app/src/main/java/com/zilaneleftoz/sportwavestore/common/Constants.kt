package com.zilaneleftoz.sportwavestore.common

object Constants {
    const val BASE_URL = "https://api.canerture.com/ecommerce/"

  /*  val retrofit: Retrofit = Retrofit.Builder()
        .baseUrl(BASE_URL)
        .addConverterFactory(GsonConverterFactory.create())
        .build()*/
}